---
name: Issue
about: A defect, gap, or decision for reconcile-rs
title: ''
labels: 'status/needs-triage'
---

<!--
AGENTS.md §9: prose is the last resort. A table, a mermaid diagram (flow,
sequence, coupling), a code block, or a link replaces a paragraph. Delete any
section below that doesn't apply — don't pad it with prose to fill it in.

Labels: `.github/labels.tsv` defines them and says what each namespace is for.
Triage replaces `status/needs-triage` with one `kind/`, one or more `area/`, and
the status this is actually waiting on; `scripts/check-issue-triage.sh` gates it.
`status/parked` is only valid if the body states what would wake the issue up.
-->

## Problem

| | |
|---|---|
| Where | `path/to/file.rs:NN` |
| What | |

## Fix

## Acceptance

- [ ]
