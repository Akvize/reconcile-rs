[package]
name = "reconcilable"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4.31", features = ["serde"] }
diff = { version = "0.1.0", path = "../diff" }
htree = { version = "0.1.0", path = "../htree" }
